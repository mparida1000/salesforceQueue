name: Deploy Apex
on:
  pull_request:
    types: [closed]
    branches: [dev]

env:
  SF_USERNAME: ${{ secrets.SF_PROD_USERNAME }}
  SF_CONSUMER_KEY: ${{ secrets.SF_CONSUMER_KEY }}
  SF_INSTANCE_URL: ${{ secrets.SF_INSTANCE_URL }}

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    # SIMPLE QUEUE - This is all you need
    concurrency:
      group: salesforce-deployments
      cancel-in-progress: true
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Setup Salesforce CLI
        run: |
          npm install -g @salesforce/cli@latest
          sf --version
      
      - name: Create JWT key file
        run: |
          echo "${{ secrets.SF_JWT_KEY }}" > server.key
          echo "JWT key created"
      
      - name: Authenticate to Salesforce
        run: |
          echo "Authenticating to Salesforce..."
          sf auth jwt grant \
            --client-id "${{ secrets.SF_CONSUMER_KEY }}" \
            --username "${{ secrets.SF_PROD_USERNAME }}" \
            --jwt-key-file server.key \
            --instance-url "${{ secrets.SF_INSTANCE_URL }}" \
            --alias github-action
          
          echo "✅ Authentication successful!"
      
      - name: Find and deploy changed Apex files
        run: |
          # Find changed files
          BASE=$(git log --pretty=format:"%H" -1 ${{ github.event.pull_request.merge_commit_sha }}^)
          HEAD=${{ github.event.pull_request.merge_commit_sha }}
          
          echo "Base commit: $BASE"
          echo "Head commit: $HEAD"
          
          CHANGED=$(git diff --name-only "$BASE" "$HEAD" | grep '\.cls$' || echo "")
          
          if [ -z "$CHANGED" ]; then
            echo "No Apex changes found"
            exit 0
          fi
          
          echo "Changed files:"
          echo "$CHANGED"
          
          # Deploy files
          sf project deploy start \
            --source-dir "$CHANGED" \
            --target-org github-action \
            --wait 30 \
            --test-level NoTestRun
          
          echo "✅ Deployment completed!"
      
      - name: Cleanup
        if: always()
        run: rm -f server.key
